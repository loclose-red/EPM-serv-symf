<form method="post" action="{{ path('pt_mesure_delete', {'id': pt_mesure.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pt_mesure.id) }}">
    <button class="btn">Delete</button>
</form>
