<form method="post" action="{{ path('capteur_delete', {'id': capteur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ capteur.id) }}">
    <button class="btn">Delete</button>
</form>
