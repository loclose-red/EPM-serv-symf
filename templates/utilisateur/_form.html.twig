<style>
    .hide {
        visibility: hidden;
    }
</style>

{{ form_start(form) }}
    {{ form_row(form.logname) }}
    {{ form_row(form.roles) }}
    {{ form_row(form.site) }}
    <div class="hide">
    {{ form_widget(form) }}
    </div>
    <button class="btn btn-outline-primary">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
